PORT=8765
# Token required in Authorization: Bearer <token>
MCP_SERVER_TOKEN=dev-token
# Forwarded by Medusa as X-MCP-Tool-Secret when invoking tools
MCP_TOOL_SECRET=change-me
# Optional: public Medusa URL (if agent is remote and cannot reach localhost)
MEDUSA_PUBLIC_URL=http://localhost:9000
